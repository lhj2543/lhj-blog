<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lhj.model.system.SysItemLocation">

	<!-- ==================================字段属性配置========================================= -->

	<!-- SYS_ITEM_LOCATION     as ""字段 -->
	<sql id="columns_SysItemLocation">
		sid  ,
		locale_id  ,
		item_cd  ,
		item_key  ,
		item_value  ,
		status  ,
		sort_key  ,
		notes  ,
		created_by  ,
		created_date  ,
		update_by  ,
		update_date  
	</sql>
	
	<!-- 新增SYS_ITEM_LOCATION 信息字段 -->
  	<sql id="columns_insertSysItemLocation">
	    (
			sid ,
			locale_id ,
			item_cd ,
			item_key ,
			item_value ,
			status ,
			sort_key ,
			notes ,
			created_by ,
			created_date ,
			update_by ,
			update_date 
	    )
  	</sql>
  
    <!-- 新增SYS_ITEM_LOCATION 传入参数值 -->
    <sql id="columns_SysItemLocationParams">
	     (
			#{sid,jdbcType=VARCHAR} ,
			#{localeId,jdbcType=VARCHAR} ,
			#{itemCd,jdbcType=VARCHAR} ,
			#{itemKey,jdbcType=VARCHAR} ,
			#{itemValue,jdbcType=VARCHAR} ,
			#{status,jdbcType=VARCHAR} ,
			#{sortKey,jdbcType=DECIMAL} ,
			#{notes,jdbcType=VARCHAR} ,
			#{createdBy,jdbcType=VARCHAR} ,
			#{createdDate,jdbcType=TIMESTAMP} ,
			#{updateBy,jdbcType=VARCHAR} ,
			#{updateDate,jdbcType=TIMESTAMP} 
	     )
    </sql>
  
	<!-- 修改SYS_ITEM_LOCATION属性字段 -->
	<sql id="SysItemLocation_update">
			sid = #{sid,jdbcType=VARCHAR} ,
			locale_id = #{localeId,jdbcType=VARCHAR} ,
			item_cd = #{itemCd,jdbcType=VARCHAR} ,
			item_key = #{itemKey,jdbcType=VARCHAR} ,
			item_value = #{itemValue,jdbcType=VARCHAR} ,
			status = #{status,jdbcType=VARCHAR} ,
			sort_key = #{sortKey,jdbcType=DECIMAL} ,
			notes = #{notes,jdbcType=VARCHAR} ,
			created_by = #{createdBy,jdbcType=VARCHAR} ,
			created_date = #{createdDate,jdbcType=TIMESTAMP} ,
			update_by = #{updateBy,jdbcType=VARCHAR} ,
			update_date = #{updateDate,jdbcType=TIMESTAMP} 
	</sql>
	
	<!-- SYS_ITEM_LOCATION 查询(包括条件按查询) -->
	<sql id="SysItemLocation_where">
	 		<if test="sid != null and sid != ''">
	 			and sid = #{sid}
	 		</if>
	 		<if test="localeId != null and localeId != ''">
	 			and locale_id = #{localeId}
	 		</if>
	 		<if test="itemCd != null and itemCd != ''">
	 			and item_cd = #{itemCd}
	 		</if>
	 		<if test="itemKey != null and itemKey != ''">
	 			and item_key = #{itemKey}
	 		</if>
	 		<if test="itemValue != null and itemValue != ''">
	 			and item_value = #{itemValue}
	 		</if>
	 		<if test="status != null and status != ''">
	 			and status = #{status}
	 		</if>
	 		<if test="sortKey != null">
	 			and sort_key = #{sortKey}
	 		</if>
	 		<if test="notes != null and notes != ''">
	 			and notes = #{notes}
	 		</if>
	 		<if test="createdDate != null">
	 			and created_date = #{createdDate}
	 		</if>
	 		<if test="updateBy != null and updateBy != ''">
	 			and update_by = #{updateBy}
	 		</if>
	 		<if test="updateDate != null">
	 			and update_date = #{updateDate}
	 		</if>
	</sql>

	<!-- SYS_ITEM_LOCATION 表关联项目信息表结果集属性声明 -->
	<resultMap type="SysItemLocation" id="SysItemLocationResultMap">
		<id column="SID" property="sid" />
			<result property="localeId" column="locale_id"/>
			<result property="itemCd" column="item_cd"/>
			<result property="itemKey" column="item_key"/>
			<result property="itemValue" column="item_value"/>
			<result property="status" column="status"/>
			<result property="sortKey" column="sort_key"/>
			<result property="notes" column="notes"/>
			<result property="createdBy" column="created_by"/>
			<result property="createdDate" column="created_date"/>
			<result property="updateBy" column="update_by"/>
			<result property="updateDate" column="update_date"/>
	</resultMap>

	<!-- ============================DML语句配置=========================================== -->

	<!-- SYS_ITEM_LOCATION 信息查询语句 -->
	<select id="findSysItemLocation" resultMap="SysItemLocationResultMap">
		SELECT 
			<include refid="columns_SysItemLocation" />
		FROM  SYS_ITEM_LOCATION 
		<where>
			<include refid="SysItemLocation_where" />
		</where>
		<!-- 通用查询排序 -->
		<include refid="com.lhj.system.orderby"/>
	</select>
	
	<!-- SYS_ITEM_LOCATION  数量查询SQL -->
	<select id="countSysItemLocation" resultType="SysItemLocation">
		SELECT count(*) total FROM  SYS_ITEM_LOCATION
		<where>
			<include refid="SysItemLocation_where" />
		</where>
	</select>

  
  	<!-- 新增SYS_ITEM_LOCATION 信息 -->
	<insert id="addSysItemLocation" parameterType="SysItemLocation">
		INSERT INTO SYS_ITEM_LOCATION <include refid="columns_insertSysItemLocation"/>
		VALUES <include refid="columns_SysItemLocationParams"/>
	</insert>
	
	<!-- 修改SYS_ITEM_LOCATION 信息 -->
	<update id="updateSysItemLocation" parameterType="SysItemLocation">
		UPDATE SYS_ITEM_LOCATION
		SET    <include refid="SysItemLocation_update" />
		WHERE  SID=#{sid}
	</update>
	
	<!-- 删除SYS_ITEM_LOCATION 信息 -->
	<update id="deleteSysItemLocation" parameterType="SysItemLocation">
		DELETE FROM SYS_ITEM_LOCATION WHERE  SID=#{sid}
	</update>
</mapper>